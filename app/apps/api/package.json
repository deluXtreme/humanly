{
  "name": "api",
  "module": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "type": "module",
  "scripts": {
    "check": "bun --eval \"await import('./index.ts'); await import('./server.ts')\"",
    "dev": "bun --watch server.ts",
    "start": "bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "world": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
