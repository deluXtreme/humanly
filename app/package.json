{
  "name": "humanly",
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "fmt": "bun run --filter 'circle' fmt"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
