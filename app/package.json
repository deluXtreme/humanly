{
  "name": "humanly",
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "arc": "bun run --filter circle arc",
    "fmt": "bun run --filter 'circle' fmt"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
